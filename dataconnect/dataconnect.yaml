specVersion: "v1"
serviceId: "advait"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "studio-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
